System Admin (Umbrella) TODO

(For before we implement UI for System Admins)

New Agency Sign Up:
    
    - set `isVerified` and `isActivated` to `true`
    - if is a DA sign up, also set Member's `isVerified` and `isActivated` to `true`